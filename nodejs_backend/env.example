# Node.js后端环境变量配置
# 复制此文件为 .env 并修改相应的值

# 服务器配置
PORT=8000
HOST=0.0.0.0

# Supabase配置
SUPABASE_URL=your_supabase_url_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
DATABASE_URL=your_database_url_here

# 微信API配置
WECHAT_API_TIMEOUT=30000

# 文件路径配置
TEMP_DIR=./tmp
UPLOAD_DIR=./uploads

# CORS配置（允许前端域名，支持通配符）
# 开发环境示例：
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001
# 生产环境示例：
# CORS_ORIGINS=http://你的域名.com,http://你的IP:3000,http://*.你的域名.com
# 允许所有域名（不推荐生产环境）：
# CORS_ORIGINS=*
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001

# 日志配置
LOG_LEVEL=info
LOG_DIR=./logs
LOG_MAX_SIZE=10485760
LOG_BACKUP_COUNT=5

# 浏览器配置
BROWSER_PROFILES_DIR=./browser_profiles

# API配置
API_PREFIX=/api/v1
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100 